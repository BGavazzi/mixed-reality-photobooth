.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
fallback_examples/
*_stdout.log
*_stderr.log
test_images/
verify_out/

# Batch output: other people's photographs, plus every intermediate the
# pipeline wrote for them. Never committed.
batch_runs/

# Scratch space for the fake ComfyUI (chaos_comfy.py)
.chaos_comfy/

# Personal interview prep / notes -- kept out of the public repo, since an
# interviewer browsing the repo shouldn't be reading the prep for their own
# interview. Remove these lines if you ever want them tracked.
INTERVIEW_PREP.md
case_study_debugging_session.md
